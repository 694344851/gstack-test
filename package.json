{
  "name": "gstack-test",
  "private": true,
  "scripts": {
    "up": "BACKEND_PORT=${BACKEND_PORT:-8010} FRONTEND_PORT=${FRONTEND_PORT:-4173} bash scripts/dev-up.sh",
    "down": "BACKEND_PORT=${BACKEND_PORT:-8010} FRONTEND_PORT=${FRONTEND_PORT:-4173} bash scripts/dev-down.sh"
  }
}
